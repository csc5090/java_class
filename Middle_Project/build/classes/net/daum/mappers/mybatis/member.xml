<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="Member"> <!-- 메퍼네임 스페이스명을 Member로 설정 -->
  
  <!-- 아이디 중복 검색 -->
   <select id="m_idcheck" parameterType="java.lang.String" resultType="member">
   <!-- parameterType은 전달인자 타입으로 생략 가능. resultType은 반환타입으로 생략 불가능.
   		resultType속성값인 member는 sqlmap.xml에서 설정한 net.daum.dto.MemberDTO 빈 클래스 객체 별칭이름이다. -->
   		select * from member where mem_id=#{id}  
   </select>
   
   <!-- 우편 주소 검색 -->
  <select id="z_list" resultType="zipcode"> <!-- parameterType 전달인자 타입은 생략함 -->
   select * from zipcode where dong=#{dong}
  </select>
  
  <!-- 회원저장 -->
     <insert id="m_in">
       insert into memberMvc (mem_id,mem_pwd,mem_name,mem_zip,mem_zip2,mem_addr,mem_addr2,mem_phone01,
      mem_phone02,mem_phone03,mail_id,mail_domain,mem_state,mem_date) values(#{mem_id},#{mem_pwd},
      #{mem_name}, #{mem_zip},#{mem_zip2},#{mem_addr},#{mem_addr2},#{mem_phone01},#{mem_phone02},
      #{mem_phone03},#{mail_id},#{mail_domain},1,sysdate)
     </insert>
     
  <!-- 암호화 된 임시비번을 수정 -->
     <update id="p_edit" parameterType="net.daum.dto.MemberDTO">
     <!-- parameterType은 전달인자 타입 속성으로 생략 가능함 -->
     	update member set mem_pwd=#{mem_pwd} where mem_id=#{mem_id}
     </update>
     
     <!-- 아이디와 회원이름을 기준으로 회원정보 검색(비번찾기) -->
     <select id="p_find" parameterType="member" resultType="member">
     	select * from member where mem_id=#{mem_id} and mem_name=#{mem_name}
     </select>
     
  <!-- 로그인 인증 처리 -> 아이디와 mem_state=1(가입회원이면1, 탈퇴면 2) -->
  	<select id="m_loginCheck" resultType="member">
  		select * from member where mem_id=#{id} and mem_state=1
  	</select>
  	
  	
  	<!-- 아이디에 대한 회원정보 구하기 -->
  	<select id="member_info" parameterType="java.lang.String" resultType="net.daum.dto.MemberDTO">
  		select * from member where mem_id=#{id}
  	</select>
  	
  	<update id="medit_ok" parameterType="member">
  		update member set mem_pwd=#{mem_pwd}, mem_name=#{mem_name}, mem_zip=#{mem_zip},
        mem_zip2=#{mem_zip2},mem_addr=#{mem_addr}, mem_addr2=#{mem_addr2},
        mem_phone01=#{mem_phone01},mem_phone02=#{mem_phone02},mem_phone03=#{mem_phone03},
        mail_id=#{mail_id}, mail_domain=#{mail_domain} where mem_id=#{mem_id}
  	</update>
  	
  	<!-- 회원탈퇴 -->
     <update id="m_del_ok" parameterType="member">
        update member set mem_delcont=#{mem_delcont}, mem_state=2, mem_deldate=sysdate
        where mem_id=#{mem_id}
     </update>

  </mapper>